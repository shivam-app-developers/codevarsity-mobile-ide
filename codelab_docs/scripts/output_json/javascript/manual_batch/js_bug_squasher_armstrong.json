{
    "difficulty": "beginner",
    "visualizerParams": {
        "visualizerType": "BugSquasherScenario",
        "visualizerParams": {
            "title": "Bug Squasher: Armstrong Power",
            "problemDescription": "The Armstrong number check is failing for 3-digit numbers because it's hardcoding the power to 2 instead of using the number of digits.",
            "language": "javascript",
            "buggyCode": "function isArmstrong(n) {\\n  const s = n.toString();\\n  let sum = 0;\\n  for (let d of s) {\\n    sum += Math.pow(parseInt(d), 2); // Bug: power should be length of string\\n  }\\n  return sum === n;\\n}",
            "solution": {
                "code": "function isArmstrong(n) {\\n  const s = n.toString();\\n  const p = s.length;\\n  let sum = 0;\\n  for (let d of s) {\\n    sum += Math.pow(parseInt(d), p);\\n  }\\n  return sum === n;\\n}",
                "explanation": "A Narcissistic (Armstrong) number is defined by the sum of its digits raised to the power of the *total count* of digits in that number."
            }
        }
    }
}