{
    "difficulty": "medium",
    "visualizerParams": {
        "visualizerType": "BugSquasherScenario",
        "visualizerParams": {
            "title": "Amicable Numbers",
            "problemDescription": "Fix the logic to correctly identify and sum amicable numbers.",
            "language": "python",
            "buggyCode": "from math import sqrt\n\n\ndef sum_of_divisors(n: int) -> int:\n    total = 0\n    # BUG: range starts at 0, which will cause division by zero\n    for i in range(n):\n        if n % i == 0:\n            total += i\n    return total\n\n\ndef solution(n: int = 10000) -> int:\n    total = 0\n    for i in range(1, n):\n        b = sum_of_divisors(i)\n        # BUG: amicable numbers must be distinct (a != b)\n        if sum_of_divisors(b) == i:\n            total += i\n    return total\n\n\nif __name__ == \"__main__\":\n    print(solution(10000))",
            "solution": {
                "code": "from math import sqrt\n\n\ndef sum_of_divisors(n: int) -> int:\n    if n <= 1:\n        return 0\n    total = 1\n    for i in range(2, int(sqrt(n)) + 1):\n        if n % i == 0:\n            if i * i == n:\n                total += i\n            else:\n                total += i + n // i\n    return total\n\n\ndef solution(n: int = 10000) -> int:\n    total = 0\n    for i in range(1, n):\n        b = sum_of_divisors(i)\n        if b != i and b < n and sum_of_divisors(b) == i:\n            total += i\n    return total\n\n\nif __name__ == \"__main__\":\n    print(solution(10000))",
                "explanation": "Fix the logic bug."
            },
            "validation": {
                "type": "run_code"
            }
        }
    }
}