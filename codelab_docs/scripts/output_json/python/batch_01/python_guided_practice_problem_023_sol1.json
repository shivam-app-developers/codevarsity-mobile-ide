{
    "difficulty": "medium",
    "visualizerParams": {
        "visualizerType": "GuidedPractice",
        "visualizerParams": {
            "mode": "snippet",
            "title": "Sum of Non-Abundant Sums",
            "description": "Calculate the sum of all positive integers which cannot be written as the sum of two abundant numbers.",
            "language": "python",
            "ghostCode": "def solution(limit: int = 28123) -> int:\n    \"\"\"\n    Calculates the sum of integers that cannot be represented as the sum of two abundant numbers.\n    \"\"\"\n    sum_divs = [1] * (limit + 1)\n    # Sieve to find sum of proper divisors\n    for i in range(2, int(limit**0.5) + 1):\n        sum_divs[i * i] += i\n        for k in range(i + 1, limit // i + 1):\n            sum_divs[k * i] += k + i\n\n    abundants = [i for i, s in enumerate(sum_divs) if i > 0 and s > i]\n    \n    is_sum_of_two_abundant = [False] * (limit + 1)\n    for i in range(len(abundants)):\n        for j in range(i, len(abundants)):\n            combined = abundants[i] + abundants[j]\n            if combined <= limit:\n                is_sum_of_two_abundant[combined] = True\n            else:\n                break\n                \n    return sum(i for i, val in enumerate(is_sum_of_two_abundant) if not val and i > 0)\n\n\nif __name__ == \"__main__\":\n    print(solution())",
            "finalCode": "def solution(limit: int = 28123) -> int:\n    \"\"\"\n    Calculates the sum of integers that cannot be represented as the sum of two abundant numbers.\n    \"\"\"\n    sum_divs = [1] * (limit + 1)\n    # Sieve to find sum of proper divisors\n    for i in range(2, int(limit**0.5) + 1):\n        sum_divs[i * i] += i\n        for k in range(i + 1, limit // i + 1):\n            sum_divs[k * i] += k + i\n\n    abundants = [i for i, s in enumerate(sum_divs) if i > 0 and s > i]\n    \n    is_sum_of_two_abundant = [False] * (limit + 1)\n    for i in range(len(abundants)):\n        for j in range(i, len(abundants)):\n            combined = abundants[i] + abundants[j]\n            if combined <= limit:\n                is_sum_of_two_abundant[combined] = True\n            else:\n                break\n                \n    return sum(i for i, val in enumerate(is_sum_of_two_abundant) if not val and i > 0)\n\n\nif __name__ == \"__main__\":\n    print(solution())",
            "validation": {
                "type": "run_code"
            }
        }
    }
}