{
    "difficulty": "beginner",
    "visualizerParams": {
        "visualizerType": "BugSquasherScenario",
        "visualizerParams": {
            "title": "Bug Squasher: Search Return",
            "problemDescription": "The search function returns true/false instead of the required index. Fix the return values.",
            "language": "javascript",
            "buggyCode": "function findIndex(arr, item) {\\n  for (let i = 0; i < arr.length; i++) {\\n    if (arr[i] === item) return true;\\n  }\\n  return false;\\n}",
            "solution": {
                "code": "function findIndex(arr, item) {\\n  for (let i = 0; i < arr.length; i++) {\\n    if (arr[i] === item) return i;\\n  }\\n  return -1;\\n}",
                "explanation": "To follow standard `indexOf` behavior, you should return the index of the found element, or `-1` if the element does not exist in the array."
            },
            "validation": {
                "type": "run_code"
            }
        }
    }
}