{
    "difficulty": "beginner",
    "visualizerParams": {
        "visualizerType": "CodeRefactorChallenge",
        "visualizerParams": {
            "title": "Code Refactor: Pointer Arithmetic",
            "problemDescription": "Refactor the linear search to use pointer arithmetic instead of array indexing.",
            "language": "c",
            "initialCode": "#include <stdio.h>\\n\\nint search(int arr[], int n, int x) {\\n    for (int i = 0; i < n; i++) {\\n        if (arr[i] == x) return i;\\n    }\\n    return -1;\\n}",
            "solution": {
                "code": "#include <stdio.h>\\n\\nint search(int *ptr, int n, int x) {\\n    for (int i = 0; i < n; i++) {\\n        if (*(ptr + i) == x) return i;\\n    }\\n    return -1;\\n}",
                "explanation": "Pointer arithmetic is a fundamental concept in C. `*(ptr + i)` is exactly equivalent to `arr[i]` but can sometimes be more efficient in certain low-level contexts."
            }
        }
    }
}