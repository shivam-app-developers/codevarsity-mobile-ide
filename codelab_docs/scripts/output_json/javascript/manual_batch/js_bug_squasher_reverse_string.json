{
    "difficulty": "beginner",
    "visualizerParams": {
        "visualizerType": "BugSquasherScenario",
        "visualizerParams": {
            "title": "Bug Squasher: Reverse Loop Index",
            "problemDescription": "The manual string reversal loop is producing an undefined character at the start. Fix the index boundary.",
            "language": "javascript",
            "buggyCode": "function reverse(s) {\\n  let res = '';\\n  for (let i = s.length; i >= 0; i--) { // Bug: starts at s.length\\n    res += s[i];\\n  }\\n  return res;\\n}",
            "solution": {
                "code": "function reverse(s) {\\n  let res = '';\\n  for (let i = s.length - 1; i >= 0; i--) {\\n    res += s[i];\\n  }\\n  return res;\\n}",
                "explanation": "Array and string indices in JavaScript are zero-based. The last valid index is `length - 1`. Starting at `length` accesses an undefined character."
            }
        }
    }
}