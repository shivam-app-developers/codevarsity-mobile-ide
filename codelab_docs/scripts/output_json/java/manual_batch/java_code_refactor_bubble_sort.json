{
    "difficulty": "medium",
    "visualizerParams": {
        "visualizerType": "CodeRefactorChallenge",
        "visualizerParams": {
            "title": "Code Refactor: Early Exit Sort",
            "problemDescription": "Optimize Bubble Sort with a 'swapped' flag to exit early if the array becomes sorted before all passes are complete.",
            "language": "java",
            "initialCode": "public class BubbleSort {\n    public static void sort(int[] arr) {\n        int n = arr.length;\n        for (int i = 0; i < n - 1; i++) {\n            for (int j = 0; j < n - i - 1; j++) {\n                if (arr[j] > arr[j + 1]) {\n                    int temp = arr[j];\n                    arr[j] = arr[j + 1];\n                    arr[j + 1] = temp;\n                }\n            }\n        }\n    }\n}",
            "solution": {
                "code": "public class BubbleSort {\n    public static void sort(int[] arr) {\n        int n = arr.length;\n        boolean swapped;\n        for (int i = 0; i < n - 1; i++) {\n            swapped = false;\n            for (int j = 0; j < n - i - 1; j++) {\n                if (arr[j] > arr[j + 1]) {\n                    int temp = arr[j];\n                    arr[j] = arr[j + 1];\n                    arr[j + 1] = temp;\n                    swapped = true;\n                }\n            }\n            if (!swapped) break;\n        }\n    }\n}",
                "explanation": "The 'swapped' flag tracks if any elements changed position. If no swaps occur in a pass, the array is already sorted, and we can stop the algorithm."
            },
            "validation": {
                "type": "run_code"
            }
        }
    }
}